[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
startCommand = "python -m uvicorn koffe.api.main:app --host 0.0.0.0 --port ${PORT:-8080}"

[[volumes]]
mountPath = "/app/data"
